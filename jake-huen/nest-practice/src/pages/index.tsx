import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'

// react-query(데이터 패칭) + recoil(전역 상태) + emotion(Style in Js)

const Home: NextPage = () => {
  return (
    <div>
    </div>
  )
}

export default Home
